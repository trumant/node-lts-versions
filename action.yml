name: Node LTS versions
author: Matt Simerson, Travis Truman
description: Get list of LTS node.js versions (active + maintenance)
runs:
  using: 'node12'
  main: 'dist/index.js'
outputs:
  lts:
    description: List of LTS versions of node.js